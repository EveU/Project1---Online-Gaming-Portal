<% @games.each do |game| %>
<div class="index-view">
  <%= image_tag game.image %>
  <br>
  <%= link_to game.name, game_detail_path(game) %>
  <br>
  <%= link_to 'New game', new_play_path(game) %>
</div>
<% end %>


