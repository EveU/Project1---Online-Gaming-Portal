<%= form_for @play do |f| %>
  <%= f.label :game_id, 'Game:' %>
  <%= f.collection_select(:game_id, @games, :id, :name) %>
  <br>
  <%= f.label :opponent_id, 'Play against:' %>
  <%= f.collection_select(:opponent_id, @users, :id, :username) %>
  <br>
  <%= f.submit %>
<% end %>
